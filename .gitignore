# Segurança — NUNCA commitar
.env
.env.*
!.env.example
*.pem
*.key
*.jks
*.p12
*.keystore
key.properties
secrets/
keys.txt
backend/keys.txt
google-services.json
!**/google-services.json.example
# Scripts locais de deploy geralmente têm creds inline enquanto se
# desenvolve. Se um dia forem versionados, use env vars e commite sem
# a extensão local (deploy-render.example.js).
deploy-render.js
deploy-render.local.*
DEPLOY_RENDER.md
firebase-debug.log

# Backups PG
*.dump
*.sql.gz
backups/

# Play Store assets gerados (SVG é fonte, PNG é output)
docs/play-store/assets/out/

# Claude Code local settings — pode conter comandos com API keys
# em Bash() permissions. launch.json é seguro (só configura preview).
.claude/settings.local.json

# Node
node_modules/
npm-debug.log*
coverage/

# Build
dist/
build/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Dados locais
*.sqlite
pgdata/
uploads/
relatorios/

# Web (Next.js) — só ignora se ainda não estava aqui
web/node_modules
web/.next
web/tsconfig.tsbuildinfo
web/.env.local
