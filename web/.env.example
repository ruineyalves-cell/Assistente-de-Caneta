# URL do backend Render (mesma que o app Flutter usa)
NEXT_PUBLIC_API_BASE=https://assistente-caneta-backend-tkl7.onrender.com

# 1 = desabilita canvas Three.js do hero (usa fallback SVG). 0 ou vazio = 3D ativo.
NEXT_PUBLIC_DISABLE_3D=0
